# http-jdk
Clojure wrapper for JDK http server

## Motivation
Occasionally, you need a no-frills, small footprint (depedency-wise), HTTP 1.1 web server.
This library provides web server facilities with the only runtime dependencies being the JRE and clojure.

## Quick Start
- TODO - add examples